# Optional extra rewrite slug for the unlisted desk. Leave empty if unused.
INTERN_DESK_SLUG=

# Linked automatically when the intern-desk Vercel Blob store is connected.
# Needed so Mark applied / notes show on every browser.
# BLOB_READ_WRITE_TOKEN=

# Kaggle live scrape uses https://www.kaggle.com/bhanageviraj on every
# /api/kaggle-stats request. No API key required. If Kaggle serves a
# recaptcha challenge instead of the profile, the site shows the last
# known snapshot with an "as of" caption instead of a Live badge.
